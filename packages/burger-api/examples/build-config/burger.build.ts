// burger.build.ts — Build-time configuration for the CLI.
// The CLI reads this file to know where to find routes and pages.
export default {
    apiDir: './src/api',
    apiPrefix: '/api',
    debug: false,
};
