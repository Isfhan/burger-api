# Providers Example

Demonstrates service injection via `burger.provide()` and `ctx.services`.

## Convention Files

- `src/providers.ts` — auto-discovered by the framework; registers services
- `src/index.ts` — clean Burger setup with no manual provide() calls

## Run

```bash
bun run src/index.ts
```
