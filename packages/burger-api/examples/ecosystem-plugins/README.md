# Ecosystem Plugins Example

Demonstrates official plugins from `ecosystem/plugins/`.

## Plugins Demonstrated

| Plugin | Purpose |
|--------|---------|
| `api-key` | API key authentication via header |
| `api-key` | API key authentication via header |

## Run

```bash
bun run src/index.ts
```
