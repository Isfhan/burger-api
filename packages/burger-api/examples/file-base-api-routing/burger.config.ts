/**
 * Build-time paths for `burger-api build`.
 * This example keeps routes under `./api` (not the CLI default `./src/api`).
 */
export default {
    apiDir: './api',
    pageDir: './pages',
    apiPrefix: '/api',
    pagePrefix: '/',
    debug: false,
};
