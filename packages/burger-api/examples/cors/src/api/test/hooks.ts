// Route-specific hooks — apply only to this route.
// Global CORS hook (src/hooks.ts) already runs for all routes.
// Add route-specific logic here if needed.
export const beforeRoute = [
    // Route-specific hooks would go here
];
