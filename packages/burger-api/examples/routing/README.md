# Routing Example

File-based routing with dynamic params `[id]`, groups `(group)`, and sub-routes.

## Run

```bash
bun link burger-api
bun run src/index.ts
```

## Test

```bash
bun test api.test.ts
```
